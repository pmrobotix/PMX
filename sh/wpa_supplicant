ctrl_interface=/var/run/wpa_supplicant
ap_scan=1

network={
        ssid="PMX2_AP_5G"
        scan_ssid=1  # only if ssid is hidden
        proto=WPA
        key_mgmt=WPA-PSK
        pairwise=TKIP
        psk="LLLLLLLL"
}

network={
        ssid="PMX5G"
        scan_ssid=1  # only if ssid is hidden
#        proto=WPA2
#        key_mgmt=WPA-EAP
#        pairwise=CCMP
        psk="LLLLLLLL"
        }
        